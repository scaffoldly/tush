# Build output: `make binary` writes the binary at the root, `make dist` the
# cross-compiled release artifacts.
/tush
/dist/

# Test and coverage output
*.test
*.out
coverage.html

# Go workspace
go.work
go.work.sum

# Editor and OS
.DS_Store
.idea/
.vscode/

# Background dev session (`make dev-start`)
/.dev.log
/.dev.pid
